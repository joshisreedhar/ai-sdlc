"""Infrastructure layer: adapters implementing the domain ports.

May import ``domain``, ``shared_kernel`` and ``contracts``. Must NOT import
``application``, ``api`` or ``apps`` (architecture rule L-03).
"""
