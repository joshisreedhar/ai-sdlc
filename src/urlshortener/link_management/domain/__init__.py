"""Domain layer: entities, value objects, errors and ports.

Depends on nothing except ``shared_kernel``, ``contracts``, the standard library and
``pydantic``. No FastAPI, no SQLAlchemy, no Redis (architecture rules L-01, L-05).
"""
